# Natural Earth source shapefiles, re-downloadable by build/geo.sh (~53 MB).
# Kept locally so rebuilds work offline; safe to delete.
build/cache/

# label-check harness output
_tmp/

# assembled by build/site.js for uploading; everything in it is a copy
site/

.DS_Store

# generated into demo/ by build/demo.js — a copy of src/ and data/
demo/ukrmap.js
demo/ukrmap-unfold.js
demo/data.js
demo/sizes.js
demo/facts.js

# generated by build/dist.js from data/; regenerate rather than edit
# (kept in the package, so not ignored: dist/)
ua-sample.svg
